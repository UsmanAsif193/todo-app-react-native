import React from "react";
import { StyleSheet, Text, View } from "react-native";

const SingleTask = () => {
  return (
    <View>
      <Text>Single Task</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default SingleTask;
